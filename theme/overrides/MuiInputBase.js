import palette from '../palette';

const MuiIconButton = {
  root: {},
  input: {
    '&::placeholder': {
      opacity: 1,
      color: palette.text.secondary
    }
  }
};

export default MuiIconButton;
